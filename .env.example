# Nombre del proyecto
SPRING_APP_NAME=CompuSearch

# Puerto interno del backend
SERVER_PORT=8080

# Base de datos
DB_HOST=mysql
DB_PORT=3306
DB_NAME=compusearch
DB_USER=root
DB_PASSWORD=200319

# Base de datos conectada con docker-compose
MYSQL_ROOT_PASSWORD=200319
MYSQL_DATABASE=compusearch

# Backup path (carpeta dentro del contenedor)
BACKUP_PATH=/var/backups

# Email
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=compusearch@gmail.com
MAIL_PASSWORD=compusearch2026

# JWT
JWT_SECRET=your_jwt_secret_here
JWT_EXPIRATION=900000
JWT_REFRESH_EXPIRATION=604800000
JWT_RESET_EXPIRATION=3600000
JWT_TOKEN_PREFIX=Bearer
JWT_HEADER=Authorization

# Stripe
# STRIPE_SECRET=your_stripe_secret_here
# STRIPE_WEBHOOK=your_stripe_webhook_secret_here

# CORS (orígenes permitidos, separados por coma)
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000